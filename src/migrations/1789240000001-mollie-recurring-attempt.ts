import { MigrationInterface, QueryRunner } from 'typeorm';
export class MollieRecurringAttempt1789240000001 implements MigrationInterface { public async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "mollie_recurring_attempt" ("createdAt" TIMESTAMP NOT NULL DEFAULT now(), "updatedAt" TIMESTAMP NOT NULL DEFAULT now(), "id" SERIAL NOT NULL, "orderId" integer NOT NULL, "molliePaymentId" character varying(64) NOT NULL, "mollieCustomerId" character varying(64) NOT NULL, "status" character varying(20) NOT NULL DEFAULT 'OPEN', CONSTRAINT "PK_mollie_recurring_attempt" PRIMARY KEY ("id"), CONSTRAINT "UQ_mollie_recurring_attempt_payment" UNIQUE ("molliePaymentId"), CONSTRAINT "FK_mollie_recurring_attempt_order" FOREIGN KEY ("orderId") REFERENCES "order"("id") ON DELETE CASCADE)`); } public async down(q: QueryRunner): Promise<void> { await q.query(`DROP TABLE "mollie_recurring_attempt"`); } }
